# 網站搬家說明（Manus → GitHub Pages）

這份資料夾是**賴薇伊老師豎琴教學網站（taiwanharp.org）的完整靜態版本**。
原本網站放在 Manus 平台上，現在已經整理成「可以直接放上 GitHub Pages 免費空間」的形式。

整包大小約 **25 MB，共 63 個檔案**（含這份說明文件）。網站外觀、文字、圖片、功能與原本完全一樣。

---

## 一、最重要的三件事（先看這裡）

1. **原本的 Manus 訂閱請「先不要」退掉。** 等新網站確定正常上線、網址也能正常打開之後，再停掉比較安全。原因請看第六節。
2. **GitHub 的 repository（專案）必須設定成 Public（公開）。** 免費帳號的 GitHub Pages 只支援公開的專案。公開的只是「網站原始檔」，這些內容本來上網就看得到，不會外洩個人資料。
3. **網址切換後，憑證（HTTPS 那把鎖）需要等候。** 通常 10 分鐘到 1 小時，最久可能要 24 小時。這段期間瀏覽器可能顯示「不安全」，是正常現象，請耐心等候，不要一直重設。

---

## 二、上傳到 GitHub

您的 GitHub 帳號看起來是 **WeiYiLai-Academic**（從瀏覽器分頁看到的），以下用這個帳號說明。

### 步驟

1. 到 <https://github.com/new> 建立一個新的 repository。
   - **Repository name**：建議取 `taiwan-site` 或 `harp-website`（英文小寫、不要空格）
   - **Public / Private**：**一定要選 Public**
   - 下面的 "Add a README file" **不要勾**
   - 按 **Create repository**

2. 建好後會看到一個空白頁面，找到 **uploading an existing file** 這個連結並點它
   （或直接進 `Add file` → `Upload files`）。

3. **把這個資料夾裡的「所有東西」拖進去。**
   - 請注意：要拖的是**資料夾裡面的內容**，不是資料夾本身。
   - 也就是說，上傳後 GitHub 上最外層應該直接看到 `index.html`、`assets`、`img` 這些項目，
     **而不是**先看到一個 `harp-site` 資料夾再點進去。這點很重要，弄錯網站會打不開。

4. 拖完後，捲到頁面最下方，按綠色的 **Commit changes**。
   檔案有 25 MB，上傳需要幾分鐘，請等它跑完。

> **關於兩個看不見的檔案**
> 這包裡有 `.nojekyll` 和 `CNAME` 兩個檔案，在 Windows 檔案總管裡可能看不到（開頭是點的檔案會被隱藏）。
> 它們很重要，請先在檔案總管上方的「檢視」勾選「隱藏的項目」，確認有一起拖上去。
> - `.nojekyll`：告訴 GitHub 不要自作聰明處理檔案
> - `CNAME`：裡面只有一行 `taiwanharp.org`，是用來綁定您自己網址的

---

## 三、開啟 GitHub Pages

1. 在剛剛建立的 repository 上方點 **Settings**（設定）
2. 左側選單找到 **Pages**
3. **Source** 選 **Deploy from a branch**
4. **Branch** 選 **main**，右邊的資料夾選 **/ (root)**，按 **Save**
5. 等 1～3 分鐘，重新整理這個頁面，上方會出現網址
   （類似 `https://weiyilai-academic.github.io/taiwan-site/`）

先用這個網址打開看看，確認網站正常顯示。

> ⚠️ 如果這時候畫面破版、圖片不見，**是正常的**，不用緊張。
> 因為網站設計成要放在網域的最上層，在 `github.io/專案名/` 這種有子資料夾的網址下會找不到圖片。
> **等第四節綁好 taiwanharp.org 之後就會完全正常。**

---

## 四、把網址 taiwanharp.org 指過來（DNS 設定）

您的網域管理在 **Global Domain Group**（名稱伺服器是 `ns1.globaldomaingroup.com` / `ns2.globaldomaingroup.com`）。

### 第 1 步：在 Global Domain Group 後台加 DNS 記錄

登入網域管理後台，找到 **DNS 管理 / DNS Records / Zone Editor** 之類的地方，加入下面 **4 筆 A 記錄**：

| 類型 | 名稱（Host / Name） | 值（Value / Points to） |
|------|--------------------|------------------------|
| A | `@` | `185.199.108.153` |
| A | `@` | `185.199.109.153` |
| A | `@` | `185.199.110.153` |
| A | `@` | `185.199.111.153` |

- `@` 代表「網域本身」，也就是 `taiwanharp.org`。有些後台是留空白、有些要打 `taiwanharp.org`，依該後台的格式填。
- **四筆都要加**，這是 GitHub 的四台伺服器，少加會變得時好時壞。
- 如果原本已經有指向 Manus 的 A 記錄或 CNAME 記錄，**請先把舊的刪掉**，否則會衝突。

（選用）想讓 `www.taiwanharp.org` 也能用，再加一筆：

| 類型 | 名稱 | 值 |
|------|------|-----|
| CNAME | `www` | `weiyilai-academic.github.io` |

> **為什麼主網域要用 A 記錄不能用 CNAME？**
> 因為 `taiwanharp.org` 是「根網域」（apex domain），技術規格上不允許設成 CNAME。
> 只有像 `www.` 這種前面有字的才可以用 CNAME。這是網際網路的規則，不是 GitHub 的限制。

### 第 2 步：回到 GitHub 設定網域

1. repository → **Settings** → **Pages**
2. **Custom domain** 欄位填入 `taiwanharp.org`，按 **Save**
3. GitHub 會開始檢查 DNS（顯示 "DNS check in progress"）

DNS 變更需要時間生效，一般 10 分鐘～2 小時，最久 48 小時。
如果顯示錯誤，先不要慌，等一兩個小時再回來看。

### 第 3 步：開啟 HTTPS

DNS 檢查通過後，同一頁下面的 **Enforce HTTPS** 就可以勾選了。

> 這個選項在憑證還沒簽發完成前會是**灰色不能點**的，這很正常。
> 請等候（通常 1 小時內，偶爾要 24 小時），憑證好了自然就能勾。
> **勾了以後，別人打 `http://` 也會自動轉到安全的 `https://`。**

---

## 五、Google Analytics（流量統計）尚未設定

原本的網站裡，Google Analytics 的編號是**假的佔位符**（`G-XXXXXXXXXX`），
它從來沒有真正在統計流量，而且原始碼裡還留著一行給工程師看的備忘註解。

**我已經把那段沒有作用的程式碼和註解移除了**，現在原始碼是乾淨的。

### 之後想要開啟流量統計的話

1. 到 <https://analytics.google.com/> 申請，取得一組「評估 ID」，格式像 `G-ABC1234XYZ`
2. 打開 `index.html`，找到**第 8 行**：
   ```html
   <!-- Google Analytics 4：尚未設定，做法見交付說明.md -->
   ```
3. 把這一行換成下面這段，並把 **兩處** `G-您的編號` 都改成您實際的 ID：
   ```html
   <script async src="https://www.googletagmanager.com/gtag/js?id=G-您的編號"></script>
   <script>
     window.dataLayer = window.dataLayer || [];
     function gtag(){dataLayer.push(arguments);}
     gtag('js', new Date());
     gtag('config', 'G-您的編號');
   </script>
   ```

> 提醒：網站共有 15 個頁面檔案（`index.html`、`404.html`，以及 `en`、`courses`、`knowledge` 等資料夾裡的 `index.html`）。
> 如果要讓每一頁都能統計，這段要貼到每一個檔案的同一個位置。
> 如果覺得麻煩，可以之後再請人協助，或只先加在 `index.html`（首頁）。

---

## 六、已知狀況與注意事項（請務必看過）

### 1. 背景音樂目前是壞的 —— 這不是搬家造成的

網站程式裡有一段會播放背景音樂（檔名 `超好聽豎琴(1)_acd2e5c0.mp3`）。

**這個音樂檔在原本的 Manus 網站上就已經不存在了**（伺服器回應「找不到檔案」）。
我特地去比對過，原網站現在也一樣沒有聲音，只是程式寫成「播不出來就安靜略過」，所以沒有人發現。

- **想要有背景音樂** → 請把那個 mp3 檔案提供給我，放進 `img` 資料夾即可恢復。
- **不想要背景音樂** → 什麼都不用做，維持現狀就好（網頁自動播音樂常常會讓訪客反感，其實不見得是壞事）。

### 2. 英文頁的標題是中文的 —— 這也是原本就有的問題

打開 `/en` 英文頁時，瀏覽器分頁標題仍顯示中文「賴薇伊 Wei-Yi Lai｜台中豎琴課…」，
而且它告訴 Google「我的正式網址是中文首頁」。這對英文版的搜尋排名不利。

**我已確認原網站也是完全一樣的狀況**，不是搬家造成的。
這需要修改網站程式才能解決，不在這次搬家範圍內。如果您在意英文版的 Google 排名，可以之後另外處理。

### 3. 有一個報名連結仍然指向 Manus

「影音作品／演出」頁面裡，有一場音樂會（Hope豎琴公益樂團 × 文化部）的**報名連結**
指向 `https://xianyu-tong-ihowuyg4.manus.space/`，這是另一個放在 Manus 上的小網頁。

**如果您停掉 Manus 訂閱，這個報名連結就會失效。**
（這是第一節說「先不要退訂」的主要原因之一。）
如果那場音樂會已經結束、連結用不到了，就不用理會。

### 4. 網站上的圖片與檔案已經全部「搬回家」了

原本網站的圖片是放在 Manus 的圖床上，其中一部分還是「有時效的臨時網址」，
**Manus 一旦停用，那些圖片就會全部消失。**

我已經把全部 **24 張圖片 + 2 份 PDF**（Aoyama 豎琴型錄、音樂會節目單）下載下來，
放進 `img` 資料夾，並把網站程式改成讀取本機檔案。
現在網站**完全不依賴 Manus 的圖床**，不會有圖片突然不見的風險。

### 5. 網域每年要續費

`taiwanharp.org` 這個網址是**每年**要付費續約的（在 Global Domain Group）。
GitHub Pages 是免費的，但**網址不是**。忘記續費網站就會打不開，建議設個行事曆提醒。

### 6. 這個網站沒有任何「表單」或後台

網站是純展示型的，沒有線上報名表單、沒有留言板、沒有資料庫。
訪客聯絡您的方式是**打電話**和 **LINE**（畫面上的按鈕），這些都是直接連出去的，不受搬家影響。

---

## 七、以後想修改網站內容怎麼辦？

**可以直接改文字的部分**：`index.html` 裡有網站標題、描述、關鍵字這些給 Google 看的資料，
用記事本打開就能改，改完再上傳到 GitHub 覆蓋即可。

**版面、課程內容、圖片這些不行直接改**：因為網站是用程式「打包」過的，
實際的內容藏在 `assets` 資料夾裡的程式檔中，那些檔案被壓縮成一長串看不懂的文字，不適合手動修改。

> 如果之後要做比較大的改版，建議的做法是：
> 請當初做網站的人（或 Manus）提供**原始程式碼專案**，
> 有了原始檔才能好好地修改與重新打包。目前這包是「成品」，適合直接上線，不適合持續開發。

換圖片是可以的：把新圖片放進 `img` 資料夾，**檔名取成跟舊的一模一樣**去覆蓋，就會換掉。

---

## 八、這包裡面有什麼

```
index.html          首頁（也是所有頁面的骨架）
404.html            找不到頁面時顯示（GitHub Pages 需要）
CNAME               您的網址設定（內容：taiwanharp.org）
.nojekyll           告訴 GitHub 原樣提供檔案
favicon.png / .ico  瀏覽器分頁上的小圖示
assets/             網站程式與樣式（16 個 js + 1 個 css）
img/                24 張圖片 + 2 份 PDF
en/                 英文版頁面
courses/            課程頁（harp / senior / teacher）
knowledge/          豎琴知識中心（6 篇文章）
performances/       演出作品頁
privacy/            隱私權政策頁
```

`en`、`courses`、`knowledge` 等資料夾裡各有一個 `index.html`。
這是為了讓 Google 能正確收錄每一頁（直接打開或重新整理都不會出錯），請不要刪除。

---

## 九、上線後請自己檢查這幾項

網址生效後，請打開 `https://taiwanharp.org/` 依序確認：

- [ ] 首頁圖片正常顯示、往下捲動內容都在
- [ ] 上方選單每個項目都點得開（課程資訊、關於賴薇伊、HopeHarp 豎琴…）
- [ ] 右上角切換到 **EN** 英文版正常
- [ ] 豎琴知識中心的 6 篇文章都打得開
- [ ] **在文章頁按「重新整理」不會出現錯誤頁面**（這項最重要）
- [ ] 手機上打開也正常（版面會自動縮成手機版）
- [ ] 網址列有出現鎖頭圖示（HTTPS）

如果有任何一項不對，先不要退掉 Manus，把狀況拍下來回報即可。
